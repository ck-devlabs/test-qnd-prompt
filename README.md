
For each premises block, extract the numeric value directly beneath "LOC #" and directly beneath "BLD #" before reading any other field. A value in the left-side numeric cell counts as explicitly present even if OCR places it on a separate line.

Do not decide that BLD # is blank until you have checked the left-side BLD # cell in that block specifically.
